---
interface Props {
    href: string;
    styles?: string;
}

const {href, styles} = Astro.props;
---

<a class=`text-linkcolor hover:underline ${styles}` href={href} target="_blank" rel="noopener noreferrer">
    <slot>
</a>